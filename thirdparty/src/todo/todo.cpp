#include "../functions/functions.h"

int main() {
    setlocale (LC_ALL, "rus");
    header (0);

 /*   ifstream read; 
    read.open("id.txt"); 

    if (!read.fail()) { 
        read >> ID; 
    }
    else {
        ID = 0;
    }
    read.close(); 
*/
    startMenu();
}